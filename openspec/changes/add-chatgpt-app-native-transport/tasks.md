## 1. Runtime Contract

- [x] 1.1 Inventory the current signed-in ChatGPT module/runtime surface using a leased background tab and identify a uniquely fingerprinted first-party conversation action
- [x] 1.2 Implement fixed runtime action resolution, invocation, bounded response observation, and cleanup in the Chrome extension without DOM input or clicks

## 2. Bridge Integration

- [x] 2.1 Add validated `runtime` and `raw` transport selection with `runtime` as the default
- [x] 2.2 Preserve extended native-host timeouts, prompt redaction, stable results, and classified handoff uncertainty

## 3. Verification and Documentation

- [x] 3.1 Add automated coverage for runtime-native success, changed runtime, ambiguity, secret containment, no UI fallback, and explicit raw mode
- [x] 3.2 Update the MCP/HTTP operator documentation and security notes
- [x] 3.3 Run the focused and full test suites plus strict OpenSpec validation
- [x] 3.4 Reload MDB and its unpacked extension, then complete one harmless signed-in end-to-end verification
